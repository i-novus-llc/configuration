spring:
  application:
    name: config-service
  cloud:
    consul:
      host: consul-agent.local
      port: 8500
      config:
        prefix: config
        defaultContext: application
        enabled: ${config.server.enable}
